{
  "_": "Read at runtime, so a deployed viewer can be repointed without a rebuild. `repo: null` means: derive it from the Pages hostname, which is right for the repository this site is published from. Set it to an `owner/name` string to watch a different public forum. `live: false` pins the page to the snapshot bundled with the build.",
  "repo": null,
  "branch": "forum-state",
  "path": "api",
  "pollSeconds": 45,
  "live": true
}
